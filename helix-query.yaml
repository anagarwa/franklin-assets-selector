indices:
  assets-index:
    target: /assets-index.json
    properties:
      polaris-assets:
        select: img
        values: |
          match(attribute(el, 'src'), 'https:\/\/[^\/]+\/adobe\/assets\/urn:aaid:aem:.*')
